{#
/**
 * Example view template override in child theme.
 * Copied from cwd_project on 2022-09-23.
 *
 * What we're doing: Add to view wrapper classes.
 */
#}

{% set view_add_classes = ['cwd-news','tiles','max-3-1'] %}
{% include "@example_base/views-view.html.twig" with {
  'view_add_classes': view_add_classes,
} %}
